{
  "name": "browser-agent",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["shared", "player", "cli", "extension"],
  "scripts": {
    "build": "tsc -b",
    "build:extension": "npm run build -w extension",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
